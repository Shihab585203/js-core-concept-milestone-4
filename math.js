const result = Math.pow(2, 4);

// console.log(result);

// abs || always showing positive result

// num1 = 20;
// num2 = 35;

// gap = Math.abs(num1 - num2);

// if(gap < 16) {
//     console.log("You guys can be friends");
// } else {
//     console.log("You guys stay apart");
// }

// math round || next full number if it decimal upper than 0.5

const numb = 3.34535;
const numbRound = Math.round(numb);
// math round || next full number if it decimal immidiate next number
const numbRound2 = Math.ceil(numb);

// math floor || next full number if it decimal immidiate previous number
const result4 = Math.floor(numb);

// *&* Random *&*

const rand = Math.round(Math.random() * 100);
// console.log(rand);

